# Kalinga Honey environment variables
# Copy to .env.local and fill in when available.

# Admin dashboard access (TBA — replace before deploy)
ADMIN_TOKEN=replace-me-with-a-long-random-string

# Local SQLite database file (created automatically)
DATABASE_PATH=./data/kalinga.db

# Razorpay (TBA)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Notification email (TBA)
NOTIFY_EMAIL=
